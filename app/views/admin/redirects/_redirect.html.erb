<div class="table-row">
  <div class="item-title cell">
    <%= link_to "#{redirect.from} to #{redirect.to}", edit_admin_redirect_path(redirect) %>
  </div>
  <div class="item-destroy">
    <%= link_to 'Delete', admin_redirect_path(redirect), method: :delete, data: { confirm: "Are you sure?" }  %>
  </div>
</div>
