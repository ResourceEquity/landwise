<div class="table-row">
  <div class="item-title cell">
    <%= link_to subject.title, edit_admin_subject_path(subject) %>
  </div>
  <div class="item-destroy">
    <%= link_to 'Delete', admin_subject_path(subject), method: :delete, data: { confirm: "Are you sure?" }  %>
  </div>
</div>
