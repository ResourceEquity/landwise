<div class="table-row">
  <div class="item-title cell">
    <%= link_to record.title, edit_admin_record_path(record) %>
  </div>
  <div class="item-destroy">
    <%= link_to 'Delete', admin_record_path(record), method: :delete, data: { confirm: "Are you sure?" }  %>
  </div>
</div>
