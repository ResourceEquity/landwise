<% if @resource.unconfirmed_email.present? %>

<h1>Is that you, <%= @resource.first_name %>?</h1>
<p>Someone recently requested a change to the email address associated with your LandWise account. To confirm this change, please click the link below.</p>
<p><%= link_to 'Confirm New Email', confirmation_url(@resource, confirmation_token: @token), class: 'button-green button-large' %></p>
<p class="expiration">Confirmation Expires: <%= (@resource.confirmation_sent_at + Devise.confirm_within).strftime('%B %-d, %Y') %></p>
<p class="ignore">If you did not request a change to your email address you can safely ignore this email. Your email won't change unless you confirm the change.</p>

<% else %>

<h1>You're almost there...</h1>
<p>In order to get started, we first need to be sure it's you. Please confirm your LandWise account by clicking the button below.</p>
<p><%= link_to 'Confirm My Account', confirmation_url(@resource, confirmation_token: @token), class: 'button-green button-large' %></p>
<p class="expiration">Confirmation Expires: <%= (@resource.confirmation_sent_at + Devise.confirm_within).strftime('%B %-d, %Y') %></p>
<p class="ignore">If you did not sign up for this account you can ignore this email and the account will be deleted.</p>

<% end %>
